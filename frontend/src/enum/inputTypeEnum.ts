/**
 * Tipos de control admitidos por los filtros del panel lateral.
 */
export enum InputTypeEnum {
  /** Casilla de verificación (permite múltiples selecciones). */
  CHECKBOX = 'checkbox',
  /** Botón de opción (permite una sola selección). */
  RADIO = 'radio'
}
